name: Deploy site to Cloudflare Workers

on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v3
      - name: Publish app
        working-directory: ./worker
        uses: cloudflare/wrangler-action@2.0.0
        with:
          apiToken: keg3T5RgXjyvUbds_ECMfgP29IYfNFOlC44DB3p4
          command: publish

