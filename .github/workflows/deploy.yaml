name: Deploy site to Cloudflare Workers

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Deploy Cloudflare Worker
    defaults:
      run:
        working-directory: ./worker
    steps:
      - uses: actions/checkout@v2.0.0
      - name: install dependencies
        run: 'yarn install'
      - name: deploy using wrangler
        run: 'yarn i @cloudflare/wrangler g'
        run: 'yarn deploy'
        env:
          CF_API_TOKEN: 'keg3T5RgXjyvUbds_ECMfgP29IYfNFOlC44DB3p4'
