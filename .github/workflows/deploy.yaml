name: Deploy

on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Publish
        uses: cloudflare/wrangler-action@2.0.0
        with:
          apiToken: 'keg3T5RgXjyvUbds_ECMfgP29IYfNFOlC44DB3p4'
          workingDirectory: 'edge-worker-cloudflare'
